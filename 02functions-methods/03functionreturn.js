// When a function returns a value , we need to do is store that value in some kind of variable.

const calArea = function(radius){
    return 3.14 * radius **2;
};

const area = calArea(5);
console.log(area);